  # Path for the classifier.
- classifier_file_name: gg-13-8-99-515-806-nb-classifier.qza

  # Qiime2 environment name.
  env: qiime2-2022.2

  # Conda path
  conda_path: /Users/dummy/miniconda/bin/conda

  # Path for Qiime2 private results
  private_results_path: path_to_private_results

  # Path for Qiime2 public results
  public_results_path: path_to_public_results

  # Logging level for the logger (CRITICAL, ERROR, WARNING, INFO, DEBUG, or NOTSET)
  logging_level: DEBUG

  # Database server
  db_host: end-point

  # Database port
  db_port: 5432

  # Database username
  db_username: username

  # Database password
  db_password: password

  # Database name
  db_name: DBName

  # Database metadata table
  db_table_metadata: metadata

  # Database Qiime2 results table
  db_table_results: results

  # Database status table
  db_table_status: status